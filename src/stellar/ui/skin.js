/*
 * Stellar Command — font strings for the cockpit reskin. One place to swap faces.
 * Loaded via the Google Fonts <link> in index.html (Part 0.2).
 *   display → wide techy caps (was Michroma)
 *   body    → News-Gothic-like condensed (was Exo 2), used for crawl + lede
 *   mono    → data readouts (unchanged)
 */
export const FONT = {
  display: "'Chakra Petch', 'Arial Narrow', sans-serif",
  body: "'Oswald', 'Arial Narrow', sans-serif",
  mono: "'JetBrains Mono', ui-monospace, monospace",
};
